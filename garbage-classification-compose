version: "3.8"

services:
    spark:
        build: ./spark
        container_name: spark
        volumes:
            - pyspark_raw:/mnt

volumes:
    pyspark_raw:
        name: pyspark_raw
